#define EVENTS true

#define DISPLAY_FREQUENCY 0 // update field image once per DISPLAY_FREQUENCY game updates.
#define UPS 10 // updates per second

#define N 64

using Field = std::array<std::array<bool, N>, N>;